IT之家6月13日消息，苹果在iOS18系统中为开发者引入了名为LockedCameraCapture的全新框架，<cand>意味着</cand>用户可以直接在锁屏界面打开第三方相机应用。
苹果iOS18系统扩展了控制中心，支持用户添加第三方应用程序，便于用户快速访问。<a>此外</a>控制中心<a>还</a>整合了锁屏和操作按钮，Halide等第三方相机应用将能够创建一个控制中心相机按钮，<cand>然后</cand>将其添加到控制中心并存储在新的控制中心图库中。
苹果公司<cand>也</cand>放开了锁屏界面上的快速按钮限制，不再是标准相机和手电筒，用户<cand>也</cand>可以选择自己喜欢的第三方应用程序，<cand>并</cand>能够选择控制中心的控件。
所有这些相互连接的最终结果是，用户可以在锁屏界面上为第三方相机应用设置一个快速访问相机按钮。
第三方相机应用通过调用特定的LockedCameraCapture框架，能在iPhone锁定状态下打开相机模式，模仿标准相机应用的功能。
<cand>而且</cand>该框架并不仅限于相机类App，<cand>只要</cand>需要调用摄像头的App都能在锁屏界面使用。IT之家援引该媒体报道，Instagram、Snapchat、TikTok等社交网络可以利用该框架，在控制中心中创建相机按钮，同理在国内环境下，锁屏界面应该<cand>也</cand>可以直接支持打开支付宝、微信的扫一扫功能，<cand>但</cand>这需要未来应用适配和更新。
